services:
  web:
    image: python:3.9
    container_name: flask_server
    volumes:
      - ./app:/app
    working_dir: /app
    command: python -m flask run --host=0.0.0.0 --port=5000
    ports:
      - "5000:5000"
    environment:
      FLASK_APP: app.py
      FLASK_ENV: development